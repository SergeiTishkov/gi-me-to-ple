{
  "name": "backend",
  "version": "1.0.0",
  "description": "A Node.js backend project bundled with esbuild",
  "main": "dist/index.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "start": "node dist/index.js",
    "dev": "nodemon --watch src --exec \"npm run build && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.5",
    "nodemon": "^3.0.0"
  }
}